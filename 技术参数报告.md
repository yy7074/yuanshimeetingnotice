# APSCVIR 会议管理系统技术参数报告

生成日期：2026-05-05

## 1. 项目概况

| 项目 | 参数 |
| --- | --- |
| 系统名称 | APSCVIR 会议管理系统 |
| 仓库目录 | `/Users/yy/Documents/GitHub/yuanshimeetingnotice` |
| 组成模块 | 管理后台 `admin`、后端服务 `server`、移动端 `conference_app` |
| 主要场景 | 会议活动管理、议程管理、讲者管理、参会用户管理、资料管理、签到、通知推送 |
| 当前线上管理端 | `http://139.129.23.105:3201/` |
| 当前线上接口文档 | `http://139.129.23.105:3201/api/docs` |
| 当前 App API 地址 | `http://139.129.23.105:3201/api/v1` |

## 2. 部署参数

| 项目 | 参数 |
| --- | --- |
| 目标服务器 | `139.129.23.105` |
| 部署用户 | `root` |
| 远端部署目录 | `/opt/yuanshimeetingnotice` |
| 管理端端口 | `3201` |
| 后端服务端口 | `3101` |
| Nginx 配置路径 | `/www/server/panel/vhost/nginx/yuanshimeetingnotice.conf` |
| 后端日志 | `/opt/yuanshimeetingnotice/logs/server.log` |
| 管理端构建日志 | `/opt/yuanshimeetingnotice/logs/admin-build.log` |
| 后端构建日志 | `/opt/yuanshimeetingnotice/logs/server-build.log` |
| 运行 PID 文件 | `/opt/yuanshimeetingnotice/run/server.pid` |
| 上传内容 | `admin`、`server`、`deploy.sh` |
| 不随脚本上传 | `.git`、`node_modules`、`dist`、`conference_app`、本地数据库文件 |

部署脚本执行参数：

```bash
SERVER_IP=139.129.23.105 \
SERVER_USER=root \
BACKEND_PORT=3101 \
ADMIN_PORT=3201 \
AUTH_SIMULATE_VERIFICATION=true \
AUTH_SIMULATED_CODE=0000 \
SCHEDULE_TIMEZONE=Asia/Shanghai \
./deploy.sh
```

## 3. 后端技术参数

| 项目 | 参数 |
| --- | --- |
| 框架 | NestJS 11 |
| 语言 | TypeScript |
| 运行命令 | `node dist/main` |
| 构建命令 | `npm run build` |
| API 全局前缀 | `/api/v1` |
| Swagger 文档 | `/api/docs` |
| 默认运行端口 | 环境变量 `PORT`，线上为 `3101` |
| 静态上传目录 | `server/uploads`，线上通过 `/uploads/` 暴露 |
| 受保护资料目录 | `/uploads/materials` 直接访问返回 403，需通过鉴权接口下载 |
| 全局校验 | `ValidationPipe`，启用 `whitelist`、`transform` |
| 全局异常处理 | `AllExceptionsFilter` |
| 限流 | `@nestjs/throttler`，short 20/s、medium 120/min、long 2000/hour |
| 定时任务 | `@nestjs/schedule` |

主要依赖：

| 类型 | 依赖 |
| --- | --- |
| Web 框架 | `@nestjs/common`、`@nestjs/core`、`@nestjs/platform-express` |
| 数据访问 | `typeorm`、`@nestjs/typeorm`、`better-sqlite3`、`pg` |
| 认证 | `@nestjs/jwt`、`passport`、`passport-jwt`、`bcryptjs` |
| 接口文档 | `@nestjs/swagger` |
| 文件与导出 | `multer`、`xlsx`、`file-saver` |
| 通知 | `nodemailer`、`firebase-admin`、`jpush-async` |
| 缓存 | `@nestjs/cache-manager`、`cache-manager`、`ioredis` |

## 4. 数据库参数

| 项目 | 参数 |
| --- | --- |
| 当前部署默认数据库 | SQLite |
| SQLite 驱动 | `better-sqlite3` |
| 线上数据库文件 | `/opt/yuanshimeetingnotice/data/conference.db` |
| ORM | TypeORM |
| 自动加载实体 | `autoLoadEntities: true` |
| 生产环境同步策略 | `DB_SYNCHRONIZE=false`，生产环境强制不自动同步 |
| 可选生产数据库 | PostgreSQL，使用 `DB_TYPE=postgres` 切换 |

核心数据表：

| 表 | 用途 |
| --- | --- |
| `users` | 用户、角色、组织、语言、推送设置、密码变更状态 |
| `events` | 会议活动、地点、时间、主办方、状态、订阅关系 |
| `sessions` | 会议议程、房间、时间、讲者、浏览统计 |
| `speakers` | 讲者资料、职位、组织、简介、头像 |
| `materials` | 会议资料、文件地址、大小、类型、下载统计 |
| `check_ins` | 签到码、签到状态、用户与活动关联 |
| `notifications` | 系统通知、活动通知、阅读状态 |

## 5. 后端接口模块

| 模块 | 基础路径 | 说明 |
| --- | --- | --- |
| 认证 | `/api/v1/auth` | 验证码、注册、登录、找回密码、个人信息 |
| 用户 | `/api/v1/users` | 用户增删改查、统计、批量状态、批量角色、导入导出、邀请 |
| 活动 | `/api/v1/events` | 活动列表、详情、统计、地区、订阅与取消订阅 |
| 议程 | `/api/v1/events/:eventId/sessions` | 活动下议程列表、详情、创建、更新、删除 |
| 议程统计 | `/api/v1/sessions/popular` | 热门议程 |
| 讲者 | `/api/v1/speakers` | 讲者列表、详情、创建、更新、删除 |
| 资料 | `/api/v1/events/:eventId/materials` | 资料列表、上传、更新、删除、下载、鉴权文件访问 |
| 签到 | `/api/v1/check-in` | 生成签到码、验证签到、我的签到、活动签到列表、签到统计 |
| 通知 | `/api/v1/notifications` | 通知列表、未读数、已读、推送设置、发送、广播 |
| 上传 | `/api/v1/upload/file` | 通用文件上传 |
| 健康检查 | `/api/v1/health` | 服务健康状态 |

## 6. 管理后台技术参数

| 项目 | 参数 |
| --- | --- |
| 框架 | React 19 |
| 构建工具 | Vite 8 |
| 语言 | TypeScript |
| UI 组件 | Ant Design 5、Ant Design Pro Components |
| 数据请求 | Axios、TanStack React Query |
| 路由 | React Router DOM 6 |
| 国际化 | i18next、react-i18next |
| 图表 | Ant Design Charts |
| 二维码扫描 | `@zxing/browser`、`@zxing/library` |
| PDF/图片导出 | `jspdf`、`html2canvas` |
| Excel 导入导出 | `xlsx`、`file-saver` |
| 本地开发命令 | `npm run dev` |
| 生产构建命令 | `npm run build` |
| API 基址 | `VITE_API_URL`，生产构建为 `/api/v1` |
| 请求超时 | 10 秒 |
| 鉴权方式 | `localStorage.token` 注入 `Authorization: Bearer <token>` |

## 7. 移动端技术参数

| 项目 | 参数 |
| --- | --- |
| 框架 | Flutter |
| Dart SDK | `^3.10.0` |
| App 名称 | APSCVIR |
| 版本 | `1.0.0+1` |
| 状态管理与路由 | GetX |
| 本地存储 | shared_preferences |
| 本地通知 | flutter_local_notifications |
| 推送 | jpush_flutter |
| 日历 | add_2_calendar |
| 二维码 | qr_flutter |
| 分享 | share_plus |
| 文件路径 | path_provider |
| 拼音检索 | lpinyin |
| 默认接口 | `http://139.129.23.105:3201/api/v1` |
| 接口覆盖方式 | `--dart-define=API_BASE_URL=...` |

移动端主要页面：

| 路由 | 页面 |
| --- | --- |
| `/startup` | 启动页 |
| `/login` | 登录 |
| `/main` | 主导航 |
| `/event_portal` | 活动门户 |
| `/event_agenda` | 活动议程 |
| `/my_schedule` | 我的日程 |
| `/digital_check_in` | 数字签到 |
| `/register` | 注册 |
| `/change_password` | 修改密码 |
| `/notifications` | 通知 |
| `/profile_edit` | 编辑资料 |
| `/speaker_detail` | 讲者详情 |

## 8. 安全与访问控制参数

| 项目 | 参数 |
| --- | --- |
| 登录认证 | JWT Bearer Token |
| 密码加密 | bcryptjs |
| 管理后台鉴权 | Axios 请求拦截器自动注入 Bearer Token |
| Token 失效处理 | 后端返回 401 时前端清除 token 并跳转登录 |
| 强制改密 | 后端返回 `PASSWORD_CHANGE_REQUIRED` 时跳转修改密码页 |
| CORS | 生产环境必须配置显式 `CORS_ORIGIN`，不允许未设置或通配 |
| 资料访问 | 资料文件目录禁止直接访问，通过鉴权下载接口访问 |
| API 限流 | 全局 ThrottlerGuard |
| Swagger 鉴权 | 支持 Bearer Auth |

## 9. 环境变量清单

| 变量 | 用途 |
| --- | --- |
| `NODE_ENV` | 运行环境，生产为 `production` |
| `PORT` | 后端监听端口 |
| `API_PREFIX` | API 前缀，默认 `api/v1` |
| `CORS_ORIGIN` | 生产 CORS 白名单 |
| `DB_TYPE` | 数据库类型，`sqlite` 或 `postgres` |
| `DB_DATABASE` | SQLite 文件路径或 PostgreSQL 数据库名 |
| `DB_HOST` | PostgreSQL 主机 |
| `DB_PORT` | PostgreSQL 端口 |
| `DB_USERNAME` | PostgreSQL 用户名 |
| `DB_PASSWORD` | PostgreSQL 密码 |
| `DB_SYNCHRONIZE` | TypeORM 自动同步开关，生产强制关闭 |
| `JWT_SECRET` | JWT 签名密钥 |
| `JWT_EXPIRES_IN` | JWT 过期时间 |
| `SMTP_HOST`、`SMTP_PORT`、`SMTP_USER`、`SMTP_PASS` | 邮件发送配置 |
| `SMTP_FROM`、`SMTP_FROM_NAME` | 邮件发件人配置 |
| `JPUSH_APP_KEY`、`JPUSH_MASTER_SECRET` | 极光推送配置 |
| `AUTH_SIMULATE_VERIFICATION` | 是否启用验证码模拟 |
| `AUTH_SIMULATED_CODE` | 模拟验证码 |
| `SCHEDULE_TIMEZONE` | 日程提醒时区 |
| `APP_DOWNLOAD_URL` | App 下载地址 |

## 10. 运维建议

1. 生产库保持 `DB_SYNCHRONIZE=false`，涉及实体字段变更时先编写迁移或手工 SQL。
2. SQLite 当前数据库文件应定期备份，建议每天备份 `/opt/yuanshimeetingnotice/data/conference.db`。
3. 正式对外使用前建议绑定域名并启用 HTTPS，再将 `CORS_ORIGIN` 调整为 HTTPS 域名。
4. 管理端和 App 的 API 地址需保持一致；当前 App 默认指向 `http://139.129.23.105:3201/api/v1`。
5. 不应在报告、代码提交或截图中暴露服务器密码、JWT 密钥、SMTP 密码、推送密钥。
6. 每次部署后至少验证：管理端首页、`/api/docs`、登录接口、活动列表接口、资料下载接口、签到接口。
